// use tmelcrypt::HashVal;

// /// A persistent tree datastructure, with every HashVal node having something bound to it.
// #[derive(Clone, Debug)]
// pub struct ImTree {
//     idx_to_hash: im::HashMap<u32, HashVal>,

//     free_idx: u32,
// }
